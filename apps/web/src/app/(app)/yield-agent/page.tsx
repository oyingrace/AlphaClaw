import { YieldAgentContent } from './_components/yield-agent-content';

export const metadata = {
  title: 'Yield Agent | AlphaClaw',
};

export default function YieldAgentPage() {
  return <YieldAgentContent />;
}
